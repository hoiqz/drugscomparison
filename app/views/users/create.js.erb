$('#myModal .modal-body').html("<%= escape_javascript(render :partial => 'form') %>");

$('#newuserform').submit(function() {
        $.post(this.action, $(this).serialize(), null, "script");
    return false;
});
$(function() {                       // On DOM ready
    $('#myModal').modal('show');     // Show the modal
});