
$(function () {
    var chart;
    $(document).ready(function() {
        Highcharts.setOptions({
            colors: <%=raw @generate_colors%>
        });
        chart = new Highcharts.Chart({
            chart: {
                renderTo: 'reviews_chart',
                plotBackgroundColor: null,
                plotBorderWidth: null,
                plotShadow: false
            },
            title: {
                text: 'Reviewers satisfaction towards <%= @drug.brand_name%> ?'
            },
            labels:{
                items: [
                    {
                        html: "<b>Restrictions:</b> <%= @label%>",
                        style: {
                            left: '0px',
                            top: '0px'
                        }
                    }
                ]
            },
            tooltip: {
                pointFormat: '{series.name}: <b>{point.percentage}%</b>',
                percentageDecimals: 1
            },
            plotOptions: {
                pie: {
                    allowPointSelect: true,
                    cursor: 'pointer',
                    dataLabels: {
                        enabled: true,
                        color: '#F8F8F8',
                        connectorColor: '#F8F8F8',
                        formatter: function() {
                            return '<b>'+ this.point.name +'</b>: '+ this.point.y +' reviewers';
                        }
                    }
                }
            },
            series: [{
                type: 'pie',
                name: 'Percentage of reviews',
                point: {
                    events: {
                        click: function(e) {
                            window.open(
                                    e.point.url,'_blank' // <- This is what makes it open in a new window.
                            );
                            e.preventDefault();
                        }
                    }
                },
                data: [
                    {
                        name: 'Score of 1',
                        y: <%=raw @update_values[@drug.brand_name][0] %> ,
                        url: "/searches/<%= @drug.id%>/non_form_search?satisfactory=1&<%= @urlendcoded %>"
                    },
                    {
                        name: 'Score of 2',
                        y: <%=raw @update_values[@drug.brand_name][1] %> ,
                        url: "/searches/<%= @drug.id%>/non_form_search?satisfactory=2&<%= @urlendcoded %>"
                    },
                    {
                        name: 'Score of 3',
                        y: <%=raw @update_values[@drug.brand_name][2] %> ,
                        url: "/searches/<%= @drug.id%>/non_form_search?satisfactory=3&<%= @urlendcoded %>"
                    },
                    {
                        name: 'Score of 4',
                        y: <%=raw @update_values[@drug.brand_name][3] %> ,
                        url: "/searches/<%= @drug.id%>/non_form_search?satisfactory=4&<%= @urlendcoded %>"
                    },
                    {
                        name: 'Score of5',
                        y: <%=raw @update_values[@drug.brand_name][4] %> ,
                        url: "/searches/<%= @drug.id%>/non_form_search?satisfactory=5&<%= @urlendcoded %>"
                    }

                ]
            }]
        });
        $('#conditions').html("<%= escape_javascript(render "satisfactory_view_form") %>");
    });

});