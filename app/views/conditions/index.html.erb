

<div class="span8 widget_container">
  <span class="miniheader">All Conditions</span>
  <article class="articlecontent">
    <h1>Conditions  A-Z</h1>
    <div class="pagination pagination-mini pagination-centered">
      <ul>
        <% @alphabetical.each do |letter| %>
            <%= content_tag :li, link_to(letter, conditions_pagination_path(:letter=>letter.downcase)) %>
        <% end %>
      </ul>
    </div>
    <section id="alphabet_listing">
      <% if params[:letter].present? %>
          <h1 class="span8 static_divider"><em><%= params[:letter].capitalize %></em></h1>
      <% else %>
          <h1 class="span8 static_divider"><em>A</em></h1>
      <% end %>

      <% if @conditions.present? %>
      <div class=" span3">
        <ul class="unstyled">
          <% @conditions[0..@sizehalf].each do |condition|%>
              <li><%= link_to condition.name, condition %></li>
          <%end%>
        </ul>
      </div>
      <div class=" span3">
        <ul class="unstyled">
          <% @conditions[@sizehalf +1 ..@size].each do |condition|%>
              <li><%= link_to condition.name, condition %></li>
          <% end %>
        </ul>
      </div>
          <% end %>
    </section>
  </article>
</div>

<article class="span2 widget_container side_widget">
  <!--<h4 class="static_divider">Popular Searches</h4>-->
  <ul class="nav nav-list">
    <section><h5 class="static_divider">Day To Day Health</h5></section>
    <% @day2day_condition.each do |entry| %>
        <li ><%= display_hits entry %></li>
     <% end %>
    <section><h5 class="static_divider">Common Health Conditions</h5></section>
    <% @common.each do |entry| %>
        <li><%= display_hits entry %></li>
    <% end %>
    <section><h5 class="static_divider">Elderly Related Conditions</h5></section>
    <% @elderly_condition.each do |entry| %>
        <li><%= display_hits entry %></li>
    <% end %>
    <section><h5 class="static_divider">Kids Related Conditions</h5></section>
    <% @kids_condition.each do |entry| %>
        <li><%= display_hits entry %></li>
    <% end %>
    <!--<li class="nav-header">Mental Conditions</li>-->
    <!--<li class="divider"></li>-->
    <section><h5 class="static_divider">Mental Conditions</h5></section>
    <% @mental_condition.each do |entry| %>
        <li><%= display_hits entry %></li>
    <% end %>
  </ul>
</article>


<article class="span2 widget_container side_widget">
  <section><h4 class="static_divider">Last Visited</h4></section>
  <ul class="nav nav-list">
    <li class="divider"></li>
    <% if session[:history].present? %>
        <% session[:history].each do |url| %>
            <li>
              <%= last_viewed url%>
            </li>
        <% end %>
    <% end%>
  </ul>
</article>


